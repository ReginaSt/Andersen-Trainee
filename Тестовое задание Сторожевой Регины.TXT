Алгоритмы - базовая теория  

Задания требуется выполнить с использованием любого из нижеперечисленных языков программирования:
(если иное не было оговорено в чате с Andersen Trainee)

JS, Java, C#, Swift, Kotlin

Задания, необходимо написать код:

1. Составить алгоритм: если введенное число больше 7, то вывести “Привет”
2. Составить алгоритм: если введенное имя совпадает с Вячеслав, то вывести “Привет, Вячеслав”, если нет, то вывести "Нет такого имени"
3. Составить алгоритм: на входе есть числовой массив, необходимо вывести элементы массива кратные 3

4. Задание, ответ в текстовой форме:

Дана скобочная последовательность: [((())()(())]]
- Можно ли считать эту последовательность правильной?
- Если ответ на предыдущий вопрос “нет” - то что необходимо в ней изменить, чтоб она стала правильной?


Задание выполнено с использованием языка программирования С#.

------Задание 1

static void Main(string[] args)
{ 
	int a;
        Console.WriteLine("1. Составить алгоритм: если введенное число больше 7, то вывести “Привет”\nВведите число:");
        a = Convert.ToInt32(Console.ReadLine()); // вводим данные с клавиатуры*
        if (a > 7) //проверяем число на то, что оно больше 7 ( значение int a должно составлять от -2147483648 до 2147483647)
            {
                Console.WriteLine("Привет");
            }
        Console.ReadKey();
}

------Задание 2

static void Main(string[] args)
{
	string s1 = "Вячеслав";
        Console.WriteLine("2.  Составить алгоритм: если введенное имя совпадает с Вячеслав, то вывести “Привет, Вячеслав“, если нет, то вывести “Нет такого имени“\nВведите имя:");
        string s2 = Console.ReadLine();// вводим имя с клавиатуры
        if (s1 == s2)
        Console.WriteLine("Привет, Вячеслав");
        if (s1 != s2)
        Console.WriteLine("Нет такого имени");
        Console.ReadKey();
}

------Задание 3

static void Main(string[] args)
{
         int[] array = { 31, 7770, -1234, -45, -3, 890, 56, 34, 25 };
         Console.WriteLine("3. Составить алгоритм: на входе есть числовой массив, необходимо вывести элементы массива кратные 3\nИсходный массив:");
         Array.ForEach(array, Console.WriteLine);
         Console.WriteLine("Результат:");
                for (int i = 0; i < array.Length; i++)
            {
                if ((array[i] % 3) == 0)
                { 
                    Console.WriteLine(array[i] + " ");
                }
            }
         Console.ReadKey();
}

------Задание 4

Дана скобочная последовательность: [((())()(())]]
- Можно ли считать эту последовательность правильной?
- Если ответ на предыдущий вопрос “нет” - то что необходимо в ней изменить, чтоб она стала правильной?

Ответ:

[((())()(())]] нужно изменить на [((()()()))]